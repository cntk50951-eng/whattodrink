# 2026-09-05 — 用户连续两次定位到我没看到的根因（自我检讨）

## 情境
- 同一天内两次：1）沉底不生效，我说是缓存／分支，用户说“ 움직임 없음”
  后我才去翻编译产物找到 Tailwind 吞类；2）位置偏高，用户直接说出
  “按钮高度按展开后面板算的”，一句话命中隐藏占位，我才去验盒模型。
- 两次都是用户先看到本质，我跟在后面证实。

## 问题
- 我的调试顺序反了：先猜最浅的原因（缓存、分支、没保存），
  把“读编译产物／看盒模型”这种决定性验证放最后。
- harness 机制本身没问题（AGENTS.md 定了开工读 memory＋24 小时全读，
  workflow Step 8 定了逢修正必记），是 compaction 后我执行断了：
  UR1.4 开工才补读，等于前半段裸奔。

## 原因
- 视觉 bug 下意识当“没生效”，不察觉还有“生效了但被别的顶走”一层。
- 对“静默失败”（无报错无警告）缺少条件反射：先证伪 Novak（产物里到底有没有）。

## 修正
- 铁律写进肌肉：定位样式 bug，第一步永远是翻 `.next` 编译产物，
  有无规则一分钟定生死，再谈缓存分支。
- 任何“隐藏／收起／透明”元素，第一反应查它占不占位。
- compaction 后首个动作就是重读 AGENTS.md＋harness＋24h memory，
  不读完不动代码。
