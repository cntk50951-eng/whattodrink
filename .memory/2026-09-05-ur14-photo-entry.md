# 2026-09-05 — UR1.4：扇形拍照入口＋按钮沉底

## 情境
- UR1.4 原文只有三句：扇形加拍照入口（拍照＋心情合并，一个按钮）、
  扇形 UI 再优化、啤酒按钮从左侧中部挪到左下。
- 拍照功能（CameraFlow）正在别人手里改，树上有未提交的 camera 改动；
  Bento 已有拍照卡（→/camera）与心情卡（→/mood）。

## 问题
- 入口行为不明（跳页还是嵌抽屉）、心情合并的范围不明、按钮要多低不明，
  带着假设做必返工。

## 原因
- 三个都是 scope 决策，不是实现细节：跳页 vs 抽屉差一个量级的工作量；
  心情卡动不动超出地图范围；bottom 再往下会撞底部状态 pill。

## 修正
- 用 AskUserQuestion 一次问清：跳 /camera 整页、扇形只要一个拍照按钮、
  降到 bottom-14（pill 展开时隐藏让位）。整理成规范 UR（范围／边界／AC／
  后续）后才开工。
- 实现：MapFab 加 photo 格（`hot` accent 高亮 prop 区分主入口与工具）；
  跳转复用 Bento 的 `/camera`（不自己拼 locale）；pill 让位用既有 fabOpen
  状态，一行条件。
