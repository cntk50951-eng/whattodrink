# 2026-09-05 — UR2.0 返工：漏他人卡片＋9b 不全

## 情境
- UR2.0 首版只给自己的想喝卡加了头像性别，他人乾杯卡没动；
  数据文档也只同步了自己的新字段。用户两条一起打回。

## 问题
- 1）范围定错：我把“他人卡片另开 UR”写进后续，理由是“等真用户”。
  但 MOCK 四人就是全部的“其他用户”，UR2.0 的意图（多人足迹分得清）
  天然覆盖他们——“等真用户”和“显示占位”是两回事，混为一谈了。
- 2）9b 执行成了半套：只同步自己动了的字段，他人字段（同一次改动）
  漏进目录。规则要求的是“新增／改变的数据”，不是“我记得的部分”。

## 原因
- 范围决策时图省事，把不确定的部分推给“另开 UR”，没有回代入
  用户原话检验（“显示用户的头像性别”——用户是复数概念）。
- 9b 检查没有逐项走：改完代码没 grep 全部新增字段就收工。

## 修正
- `Checkin` 加 `avatarEmoji`＋`gender`（`Gender` 复用 `lib/me.ts`），
  乾杯卡头圈换人＋性别 pill＋酒 emoji 并入饮酒行（与自己卡片同构）；
  单测显示字段断言补上（泛型循环，加两行即覆盖全员）。
- 9b 补全：home-map 他人两行＋替换清单 join 口径＋backlog 推翻线。
- 铁律：范围里写“另开 UR”前，先回代入用户原话检验一遍；
  9b 收工前 grep 本轮全部新增字段，一个个对目录。
