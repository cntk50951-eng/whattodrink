# 2026-09-26 UR C.1 v2 首頁實作（Snap Map 式）

## 情境

用戶給 Snap Map 截圖＋拍板映射（足跡末格／添加好友佔位／dark 延後），開工 C.1：全屏原生地圖＋頂欄＋橫滑 pills＋右列＋底部列＋五格 tab bar，shadcn 皮，共用層邏輯。

## 問題

- 底圖：CARTO 全面要 key（調研確認，和當年水印同一回事）；OpenFreeMap 向量要 MapLibre＋bridge 兩新依賴。C.1 沿 OSM raster 免 key，零風險。
- vitest 無 config、`@/` 別名測不了（v2Pins.test 用別名即 0 測挂）：改相對引用（全庫舊測皆相對，沿既有口徑，不新建 config）。
- 測試數 237 不漲即單測沒被掃：以後加測試文件先單跑確認再看總數。

## 原因

- 新文件用別名在源碼無礙（next 配好），但測試沿舊口徑只能相對。
- 總數核對是單測落地的必要複核（本次即捉）。

## 修正

- 文件：`app/[locale]/v2/page.tsx`＋`components/v2/`（V2Home／V2MapView／v2.module.css／v2Pins＋單測4）＋v2 三語11鍵；全量新文件，v1 零文件（隔離門 `git status` 驗過）。
- 行為與 v1 一致：選酒 L1／L2／快貼帖子雙鈕／登入 link／隱身守衛／乾杯限額／邀約mock／足跡／搖一搖（按鈕觸發，真機感應 C.x）／模式 Popover；pin 卡底部 shadcn Card（非錨定）；守衛 Sheet（矩陣同 v1，卡不關）。
- 有意省略：天氣（無數據源）、dark（鎖淺色，樁位留）、聚合外之簇（clusterPoints 照用）、想喝刪改、匿名打卡續打（v1 有，v2 從簡走登入頁）。
- 三閘：237→241綠／lint 0 error（含 set-state-in-effect microtask 修一例）／build 39頁（含/v2×3）。

## 關聯

- 關聯 UR C.1 [WIP]；入口直連 `/v2`（v1 零改動）；業務邏輯零新增（全複用共用層）
