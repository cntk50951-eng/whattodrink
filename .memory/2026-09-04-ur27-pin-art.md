# 2026-09-04 UR2.7 追加：他人 pins＋别人卡酒图（iconForDrinkName）

- 用户 follow-up 两个问题都答了：① 别人打卡没放大酒图——是因为 matcher
  不存在（pin 循环只会画 `drinkEmoji`），且 Highball／Mojito／IPA 至今无
  设计稿，无图可配是诚实的；② 默认视图 pins 有图上设计稿、无图留默认。
- 新 `iconForDrinkName`（wall.ts）：`BRAND_ALIASES` 表，拉丁整词匹配
  （大小写不敏感）＋中文子串，别名按长度全局降序先比，猜不到回 null。
- 自查抓到的真 bug：“Negra Modelo”会被短 alias `modelo` 先中、错配到
  Modelo Especial——补 `negra modelo` 最长 alias。教训：别名表每加一行
  都要想“谁是它的子串”。
- pin 侧：命中→方形 `.pinArt` 钉 56px（`renderToStaticMarkup` 内联 SVG 进
  Leaflet divIcon，圆钉装不下高瓶所以换方形）；卡侧：命中→酒图 h-24 主角
  ＋头像缩右下角标（保身份，沿用用户“不猜酒”拍板精神）。
- 门：wall.test.ts 加 4 matcher 单测（命中／最长优先／未命中 null／词内
  不误配），55 tests／tsc 净／lint 0 error。
- 返工（用户验收抓漏）：自己的「想喝」pin 从第一版起就没接任何映射，
  一直画 `picked.emoji`——青島 id 对得上但 pin 没查表。以后“自己的东西
  不显示图”先查 pin 层有没有走映射，别只看卡片层。修法同构他人 pin，
  框换品牌红＋声纳圈保“想喝”身份，和他人墨色钉区分。
- 踩雷（环境）：本机 sandbox 里 `npm run build` 必挂——Turbopack CSS 管线
  起子进程绑端口被 sandbox 拦（`Operation not permitted`），不是 CSS 语法
  问题（postcss 独立 parse 通过）。以后 build 挂先看 panic 里有没有
  binding to a port，有就是环境，别改代码。待用户侧 build＋浏览器亲验。
