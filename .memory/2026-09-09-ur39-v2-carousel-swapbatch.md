# 2026-09-09 UR3.9 v2 返工（輪詢卡＋自己紀錄批次自選）

## 情境
- 用戶驗收返工三點：L1 類別 list 無現代感／非手繪風（要照片輪詢級體驗）；點自己紀錄仍彈換酒鈕（要批次內自選）；保留換下一批。

## 問題
1. v1 L1 是二列文字格（emoji＋名＋＋鈕），資訊密度有餘、視覺無主角。
2. v1 自己紀錄沿 UR3.7 盲搖換酒（點鈕即換一杯），與「批次自選」語義衝突。

## 原因
1. v1 只求把分類可點，沒給每類一張「臉」（代表酒大圖）。
2. UR3.7 換酒是單步動作，v1 沒動它，返工才暴露。

## 修正
- L1 改橫向 snap 輪詢卡：每類代表酒（該類首個有手繪 icon，否則 emoji）h-24 大圖＋膠帶貼紙＋交錯旋轉＋款數（`laneCount` 三語），點卡進 L2，角落＋鈕直打保留；橫滑 `[scrollbar-width:none]` 藏捲軸。
- `lib/beers.ts` 抽 `shuffleTake` 共用核心，`pickRandomBatch` 改調它；新增 `pickSwapBatch`（同類減當前，單類回退全局減當前）＋2 單測。
- 自己卡：換酒鈕改展開／收起（`swapOpenFor` 按 `at` 配對，切紀錄自動收，無 effect）；展開為卡內 3 列批＋換下一批（避全等重試 3 次），點格 `handleSwapTo`（沿 `swapWantBeer`，at／位置不動）；刪除兩段確認不動，刪卡清 `swapOpenFor`。
- 門：103→105 tests／tsc 淨／lint 0 error（3 舊 warning）。9b：零新增數據，無需更新。
