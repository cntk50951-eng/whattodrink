# 2026-09-07 UR3.0 碰杯特效＋乾杯双边记录

## 情境
- 用户：点乾杯直接变字，零互动感；要点一次双方各＋一条记录（数据层
  先记文档，以后实现）。

## 问题
- mount effect 里同步 `setReducedMotion(matchMedia…)` 撞
  `set-state-in-effect` error。

## 原因
- 同步写即错，不管是不是 mount 一次——规则不看意图看位置。

## 修正
- 包 microtask（`void Promise.resolve().then(…)`，UR1.8 同配方）。
- 本轮实现备忘：`cheersFx {id,key}｜null`＋`CHEERS_FX_MS = 1300`；
  特效层卡内绝对覆盖（面板即定位祖先，无 portal／z-index 事）；
  两杯复用 BeerMugDoodle（气泡传 "!" 当碰杯拟声，不动共享组件）；
  提交走 keyed effect＋timeout，计数乐观＋1，连点守卫＋disabled；
  数据口径：一行双读（`to_user_id` 新增列，不做镜像双行）。
- 门：63 tests／tsc 净／lint 0 error。9b：future-schema＋home-map 双边
  行同步（用户点名要记，EPIC 3.0 实现）。
- v2 返工（用户：不够突出／消失太快／缺手绘感）：杯 w-28＋星形冲击＋
  速度线＋5 泡沫＋碰杯颤动＋大字硬阴影，1.3s→2.2s 结尾整层淡出；
  速度线半秒内收掉（常显变 perpetual，skill 5，明禁）。
