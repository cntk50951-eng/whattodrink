# 2026-09-05 — 合并教训：解完冲突必须 grep 残留标记再 commit

## 事实
- UR1.3 合 main 时 CHANGELOG 冲突（main 上 #9 先改了同一段）。
  手工消重后只删了 `<<<<<<<`／`=======`，漏了一行结尾的 `>>>>>>>`，
  直接进了 merge commit。push 前复查发现，`--amend` 修掉（main 还没推出去）。
- 铁律：解冲突后先 `grep -c '<<<<<<<\|>>>>>>>' <file>` 确认归零，
  再 `git add`＋commit。机器不帮你检查这个。
