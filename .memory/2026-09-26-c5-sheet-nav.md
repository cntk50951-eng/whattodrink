# 2026-09-26 UR C.5 選酒弹窗導航鍵 shadcn 標準化

## 情境

用戶驗收：選酒 overlay 批量段缺顯式 close；返回鍵純文字無 chevron、角落 X 與自排 header 打架，不合 shadcn 觀感。

## 修正

- 導航行 `[‹ 返回 … X]`：左 ghost＋ChevronLeft＋文案（有上一段才現身，根段佔位保右對齊）；右 in-flow X（`SheetClose` 正規件＋`size="icon-sm"`）；`showCloseButton={false}` 關默認角落 X（Esc 照走）。
- 段內舊返回鍵兩處退役，導航行接管；零新 key；v1／共用層零動。
- 三閘：253綠／lint 0 error／build 39頁；用户手機＋桌面驗收通過。

## 並行施工（重要）

- 插畫另一 dev 同樹施工中（batch3a／batch3），BACKLOG／CHANGELOG 同文件纏繞——沿 A.20 拆車配方：備份混合檔→checkout→只重做我的 hunks→提交→原樣恢復，對方零感知。
- 對方 CHANGELOG 自述事故恢復（我的 A.20 拆車曾吞其 hunks，對方已重放）——拆車後必 grep 驗對方 hunks 還在恢復檔裡，提交前再驗一次工作區。

## 關聯

- 關聯 UR C.5 [✓]，已合入 main
